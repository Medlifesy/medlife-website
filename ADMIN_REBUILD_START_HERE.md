# Administration rebuild starts here

This branch is the safe reset point for the MedLife administration UI.

Legacy admin UI is being retired before the new admin is built.

Public website and public article reader remain outside this rebuild.
