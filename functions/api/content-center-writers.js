const NOTION_VERSION='2025-09-03';const COOKIE='medlife_content_center_session';const enc=new TextEncoder();const WRITER_DS={'Neuron Cells':'7c5df679-7ca3-479a-bb0f-016c7834c583','Brain Cells':'1653de24-5d4c-800b-9da5-000bbd1c9b79','Stem Cells':'42c000f4-799e-4b3b-90ad-68ef76809e6d','Leukocyte':'38201ea2-a074-4dfb-b707-9d6485c41260','Red Blood':'fb2da9b3-010b-45cb-955c-c6c3ef48c34d','Heart Cell':'a43004a2-9400-458c-9f11-93fbbaa06e30','Plasma Cell':'fd76750f-31ef-4fb5-8ca7-d2141db86d70'};function json(d,s=200){return new Response(JSON.stringify(d),{status:s,headers:{'content-type':'application/json; charset=utf-8','cache-control':'no-store'}})}function hex(b){return Array.from(new Uint8Array(b)).map(x=>x.toString(16).padStart(2,'0')).join('')}async function sha(v){return hex(await crypto.subtle.digest('SHA-256',enc.encode(v)))}function cookie(req){const x=req.headers.get('Cookie')||'';for(const p of x.split(';')){const q=p.trim();if(q.startsWith(COOKIE+'='))return decodeURIComponent(q.slice(COOKIE.length+1))}return null}function text(p){if(!p)return'';if(p.type==='title')return(p.title||[]).map(x=>x.plain_text||'').join('').trim();if(p.type==='rich_text')return(p.rich_text||[]).map(x=>x.plain_text||'').join('').trim();if(p.type==='select')return p.select?.name||'';return''}async function notion(env,path,init={}){if(!env.NOTION_API_TOKEN)throw Error('NOTION_API_TOKEN is not configured');const r=await fetch('https://api.notion.com'+path,{...init,headers:{Authorization:`Bearer ${env.NOTION_API_TOKEN}`,'Notion-Version':NOTION_VERSION,'Content-Type':'application/json',...(init.headers||{})}});const d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.message||d.code||`Notion ${r.status}`);return d}async function identity(req,env,db){const t=cookie(req);if(!t)return null;const row=await db.prepare("SELECT account_page_id FROM content_center_sessions WHERE token_hash=?1 AND datetime(expires_at)>datetime('now') LIMIT 1").bind(await sha(t)).first();if(!row)return null;const p=await notion(env,`/v1/pages/${row.account_page_id}`),x=p.properties||{};return{cell:text(x['الخلية']),role:text(x['الدور']),status:text(x['الحالة'])}}async function queryAll(env,ds){let out=[],cursor;for(let i=0;i<10;i++){const d=await notion(env,`/v1/data_sources/${ds}/query`,{method:'POST',body:JSON.stringify({page_size:100,...(cursor?{start_cursor:cursor}:{})})});out.push(...(d.results||[]));if(!d.has_more||!d.next_cursor)break;cursor=d.next_cursor}return out}function dsFor(cell){return WRITER_DS[String(cell||'').trim()]||null}function title(v){return{title:[{text:{content:String(v)}}]}}export async function onRequest({request,env}){if(!env.DB)return json({success:false,error:'Database binding DB is not configured.'},500);try{const me=await identity(request,env,env.DB);if(!me||me.status!=='فعّال'||!['مسؤول خلية','مشرف خلية'].includes(me.role))return json({success:false,error:'Unauthorized'},401);const ds=dsFor(me.cell);if(!ds)return json({success:false,error:`لم يتم ربط قائمة كتّاب الخلية «${me.cell||'غير محددة'}» بعد.`},404);if(request.method==='GET'){const rows=await queryAll(env,ds);const writers=rows.map(p=>({id:p.id,name:text(p.properties?.Name)||text(p.properties?.الاسم)||''})).filter(x=>x.name).sort((a,b)=>a.name.localeCompare(b.name,'ar'));return json({success:true,cell:me.cell,writers})}if(request.method==='POST'){const b=await request.json().catch(()=>({}));const name=String(b.name||'').trim();if(!name)return json({success:false,error:'اسم الكاتب مطلوب.'},400);const rows=await queryAll(env,ds);const existing=rows.find(p=>(text(p.properties?.Name)||'').trim()===name);if(existing)return json({success:true,existing:true,writer:{id:existing.id,name}});const page=await notion(env,'/v1/pages',{method:'POST',body:JSON.stringify({parent:{data_source_id:ds},properties:{Name:title(name)}})});return json({success:true,writer:{id:page.id,name}})}return json({success:false,error:'Method not allowed'},405)}catch(e){console.error('content-center-writers',e);return json({success:false,error:e instanceof Error?e.message:'تعذر تنفيذ الطلب.'},502)}}
